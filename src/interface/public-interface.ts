import PublicInterface from '../../rx-postmessenger';

export import IStatic = PublicInterface.Static;
export import IMessenger = PublicInterface.Messenger;
export import IRequest = PublicInterface.Request;
